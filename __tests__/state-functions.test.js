// simple test. To make sure it works, change expect to something false to see if test fails
describe('Addition', () => {
  it('knows that 2 and 2 make 4', () => {
    expect(2 + 2).toBe(4);
  });
});
