# No need to host the build, as it can be done simply by running the build command
**/build
# Since we are using sass, no css files required in the project
*.css
# Everyone is responsible for adding the required dependencies for building.
node_modules/*

#Ignore the database
database/aaysDB.sql

#doc files to ignore
docs/databaseDoc/*
docs/apidoc/*

#ignore enviroment variables. The other versions are files made by vim
.env
.env~
..env.un~